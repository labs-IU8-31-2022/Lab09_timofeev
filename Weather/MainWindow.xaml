<Window x:Class="Weather.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Weather"
        mc:Ignorable="d"
        Title="Weather BOT" Height="450" Width="800" d:DataContext="{d:DesignInstance }">
    <Grid>
        <StackPanel>
            <TextBlock>
                Получение погоды в городе из списка
            </TextBlock>
            <ComboBox
                x:Name="ComboBox1"
                HorizontalAlignment="Left"
                ItemsSource="{Binding Cities}"
                SelectedItem="{Binding SelectedCity}"
                IsEditable="True"
                DisplayMemberPath="Name"
                Width="250">
            </ComboBox>
            <Button x:Name="Button1" Background="Chartreuse" Width="120" Height="30" HorizontalAlignment="Left"
                    Content="Получить погоду" Click="GetApi" />
            <Button x:Name="Button3" Background="Plum" Width="120" Height="30" HorizontalAlignment="Left"
                    Content="click" Click="Count" />
            <TextBlock
                x:Name="Text1" />
        </StackPanel>
    </Grid>
</Window>